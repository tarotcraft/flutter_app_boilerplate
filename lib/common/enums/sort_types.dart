
enum RepositorySortTypes {
  stars,
  forks,
  updated,
}

enum UserSortTypes {
  followers,
  repositories,
  joined,
}

enum UserRepositorySortTypes {
  created,
  updated,
  pushed,
  // ignore: constant_identifier_names
  full_name,
}

enum UserRepositoryTypes {
  all,
  owner,
  member,
}
